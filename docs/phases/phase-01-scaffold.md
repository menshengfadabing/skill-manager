# phase-01 — scaffold + core CLI

## Done

- docs: design / stack-choice / framework-choice
- Go module + bubbletea TUI
- Commands: list, enable, disable, sync, doctor, use, save, init, profiles, paths, install-bundled
- Dual activity sets: direct symlinks to skills-all (no two-level)
- Bundled companion skills: skill-manager, skill-init
- Unit tests for enable/disable + sync two-level fix

## Not done (manual)

- Live bootstrap on `~/.agents` / `~/.claude` (`skill sync`) — run when ready; mutates real skill dirs
