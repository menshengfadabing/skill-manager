# 完整功能测试记录 — 2026-07-21

## 备份

路径：`/home/no_story/.agents-backup-20260721-114437`

- `agents/` ← `~/.agents`（含 skills + .skill-lock.json）
- `claude/skills/` ← `~/.claude/skills`

## 初始态（用户已删 skills-all 等）

- `.agents/skills`：24 个实体目录
- `.claude/skills`：28 项（实体 + 二层软链）
- `doctor`：52 error + 18 warn

## 结果

| 步骤 | 结果 |
| --- | --- |
| install-bundled | 装入 skill-manager / skill-init |
| sync | 迁入 warehouse；二层链改直连；双侧对齐 |
| doctor after sync | `no issues found` |
| 软链形态 | `agents → ../skills-all/X`，`claude → ../../.agents/skills-all/X` |
| enable/disable | 双侧同步；warehouse 保留 |
| save/use lean | OK |
| init → core | 仅 skill-manager + skill-init（双侧） |

## 测试中修复

1. `skill init` 改为每次刷新 canonical core（避免被 `save core` 污染）
2. 冲突备份改放到 `~/.agents/skills-bak/`，不再污染 warehouse 列表

## 当前活动集

已 `skill use lean`（34 enabled / 37 warehouse）。
